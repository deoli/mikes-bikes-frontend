const state = {
  configurator: {},
};

const getters = {
  stateConfigurator: state => state.configurator,
};

const actions = {
};

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations
};
